{
  "name": "li4chess",
  "private": true,
  "version": "0.0.0",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "pnpm --filter @li4chess/web dev",
    "build:workers": "pnpm --filter @li4chess/worker build:assets",
    "dev:workers": "pnpm --filter @li4chess/worker dev:local",
    "test:workers": "pnpm --filter @li4chess/worker test:runtime",
    "check:workers": "pnpm --filter @li4chess/worker check:deploy"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.3",
    "wrangler": "^4.129.0"
  },
  "packageManager": "pnpm@10.33.0"
}
