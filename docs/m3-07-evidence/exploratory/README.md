# M3-07 earlier attempts

These records are separate from corrected acceptance on `f9c5492`.

| Directory | Actual outcome and correction |
| --- | --- |
| `m3-07-explore-ordinary-1` | Passed two ordinary histories and four-member export/import. Exploratory dirty source; early host child-tool resolution could select the global pnpm 11 wrapper, so this is not the pinned full validation claim. |
| `m3-07-explore-retrieval-1` | Failed: a fresh observer tab had not explicitly joined the invitation. Corrected fixture setup. |
| `m3-07-explore-retrieval-2` | Failed during restart/rotation. Read-outage injection initially shared a gameplay binding; isolated injection to replay reads. |
| `m3-07-explore-retrieval-3` | Failed again, exposing the actual existing terminal publication problem: a closed sibling channel during session rotation suspended a committed terminal game. Scoped production correction prunes/re-publishes terminal presence, retaining active-game failure recovery. |
| `m3-07-explore-retrieval-4` | Passed 11 focused observations/3 starts on dirty source. This exploratory pass preceded final review/boundary tests. |
| `windows-c29885d` | First clean full Windows pass: all 17 commands, 695 units, 47 browser tests, 83/27 and 11/3 campaigns. Retains checks/logs/manifests/source maps; final corrected evidence retains full game artifacts without duplicating this earlier set. |
| `linux-ci-34170371852` | Failed after ordinary repetition/walking exports. Next test API POST had a socket hang-up. Idle pooled socket reset is inferred, not proven. Reviewed driver correction uses non-reused HTTP connections, zero retries and unchanged browser transport/assertions. Retains full redacted console log, failed metadata, sanitized partial histories, source map and exact CI merge object. |

Each directory's `artifact-index.json` records bytes/checksums. Dirty exploratory
source maps preserve exact code, rather than labeling those runs as current code.
The failed CI source tree matches permanent head `c29885d`; raw merge-object bytes
make its original source revision reconstructible without retaining another archive.
`log-redaction.json` records removal of only the raw GitHub console log containing
disposable fixture headers. Failed status and sanitized evidence remain intact.
[Incidental tooling failures](tooling-failures.json) distinguish runner/ESM startup
errors and the caught intermediate sanitizer test failure from runtime measurements.
